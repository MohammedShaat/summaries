# Create A Branch On The Local Repository When There Are Multiple Remotes With The Same Branch Name

`git checkout -b <branch name> <remote>/<remote branch>`

**Example**
```git
$ git checkout -b origin/feature-branch
```