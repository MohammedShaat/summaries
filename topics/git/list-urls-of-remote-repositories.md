# List URL's Of Remote Repositories

`git remote -v`

**Example**
```git
$ git remote -v
```

**result**
```git
origin  git@github.com:MohammedShaat/Summaries.git (fetch)
origin  git@github.com:MohammedShaat/Summaries.git (push)
```